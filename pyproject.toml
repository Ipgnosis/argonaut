[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "colchis"
version = "0.4.0"
description = "A small package, built around one class (Argo), for loading, validating, pretty-printing, and inspecting the structure of a JSON file."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Russell Bennett", email = "russell@beanbazaar.com" }]
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "ruff", "mypy"]

[project.urls]
Homepage = "https://github.com/Ipgnosis/colchis"
Issues = "https://github.com/Ipgnosis/colchis/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
src = ["src"]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
files = ["src"]
python_version = "3.10"
ignore_missing_imports = true
